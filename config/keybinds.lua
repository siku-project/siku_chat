KeybindsConfig = {
  --- Open chat key
  ---
  --- The key that opens the chat input.
  --- Uses FiveM key-mapping names; players can rebind it from the game settings.
  ---
  --- Default: 'T'
  openKey = 'T',

  --- Toggle passive key
  ---
  --- The key that toggles the passive overlay on/off for the player,
  --- only when PassiveConfig.mode is 'dynamic'. The choice is session
  --- only — it resets when the resource restarts.
  ---
  --- Default: 'L'
  passiveKey = 'L',
}
